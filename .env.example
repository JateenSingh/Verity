# Copy this file to .env and fill in the value shared with you separately.
# docker-compose.yml reads SEED_PASSWORD to set the password for every
# seeded demo account (see README.md "Seed accounts").
SEED_PASSWORD=
